{
  "name": "typescript-sandbox",
  "version": "0.0.0",
  "description": "Typescript sandbox",
  "scripts": {
    "start": "node ./build/31-generic-default/index.js",
    "build": "tsc",
    "clean": "tsc --build --clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "James Morse",
  "devDependencies": {
    "@types/node": "^18.11.17"
  }
}
